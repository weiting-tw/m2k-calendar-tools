{
  "name": "m2k-calendar-tools",
  "private": true,
  "version": "1.0.0",
  "description": "m2k 行事曆工具集（userscript 的離線測試在 tests/*.mjs，實機測試在 tests/live_*）",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "jsdom": "^28.0.0"
  }
}
