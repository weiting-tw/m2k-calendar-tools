{
  "name": "m2k-calendar-app",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "m2k 行事曆 MCP App UI（打包成單一 HTML 由 MCP server 以 resource 提供）",
  "scripts": {
    "build": "vite build",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/ext-apps": "^1.7.4"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vite": "^8.1.3",
    "vite-plugin-singlefile": "^2.3.3"
  }
}
