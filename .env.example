# 複製這個檔為 .env，填入你的值（.env 請勿上傳/分享）
# 用法：把本檔另存為 .env 放在 m2kcal.py 同目錄

M2K_URL=https://mail.gss.com.tw/cgi-bin/cal/caldav/
M2K_USER=you@example.com
# 注意：SAML SSO，請用「應用程式/裝置專用密碼」，不是一般登入密碼
M2K_PASS=your-app-specific-password

# （選用）webmail Cookie。查同事行程（others_agenda、find_free_slots 帶 attendees）
# 預設會用上面的 M2K_USER/M2K_PASS 自動向 /cgi-bin/login 換 session，通常不必設這個。
# 只有想用別的登入身分、或自動換失敗想手動覆蓋時才設：key=<值>
# （已登入瀏覽器 DevTools → Application → Cookies → key）。m2kgroup.py 亦讀這個。
# M2K_COOKIE=
