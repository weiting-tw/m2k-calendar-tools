# 複製這個檔為 .env，填入你的值（.env 請勿上傳/分享）
# 用法：把本檔另存為 .env 放在 m2kcal.py 同目錄

M2K_URL=https://mail.gss.com.tw/cgi-bin/cal/caldav/
M2K_USER=you@example.com
# 注意：SAML SSO，請用「應用程式/裝置專用密碼」，不是一般登入密碼
M2K_PASS=your-app-specific-password

# （選用）webmail Cookie：MCP 查同事行程（others_agenda、find_free_slots 帶 attendees）
# 與群組展開 CLI m2kgroup.py 需要。從已登入瀏覽器 DevTools → Application → Cookies
# 複製名為 key 的 cookie，寫成 key=<值>（整串也行，但只有 key 是必要的）。短效，過期重貼。
# M2K_COOKIE=
# （選用）m2kgroup.py 才需要的短效 token
# M2K_M=
# M2K_SSNID=
